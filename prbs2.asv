function out = prbs2(n,m,N)
in = [ ones(1,1), zeros(1,m-1) ];
for i=1:N
    xr(1) = xor(in(n),in(m));
    
    j=1:m-1;
    
        j=1:n-1;
    s(n+1-j)=s(n-j);
    s(1)=b(m-1);
    seq(k+1)=s(n);
end
end

